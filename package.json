{
  "name": "opencode-custom-buttons",
  "displayName": "opencode Custom Buttons",
  "description": "Configurable editor-title buttons for opencode and other CLI agents.",
  "version": "0.1.0",
  "publisher": "ALXWJJ",
  "repository": {
    "type": "git",
    "url": "https://github.com/ALXWJJ/opencode"
  },
  "license": "MIT",
  "icon": "images/icon.png",
  "galleryBanner": {
    "color": "#000000",
    "theme": "dark"
  },
  "engines": {
    "vscode": "^1.94.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "opencode.button.open",
        "title": "Open opencode",
        "icon": {
          "light": "images/button-dark.svg",
          "dark": "images/button-light.svg"
        }
      },
      {
        "command": "opencode.button.new-session",
        "title": "New opencode session",
        "icon": "$(sparkle)"
      },
      {
        "command": "opencode.button.codex",
        "title": "Open Codex",
        "icon": "$(terminal)"
      },
      {
        "command": "opencode.addFilepathToTerminal",
        "title": "Add Filepath to Terminal"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "opencode.button.open",
          "group": "navigation@1"
        },
        {
          "command": "opencode.button.new-session",
          "group": "navigation@2"
        },
        {
          "command": "opencode.button.codex",
          "group": "navigation@3"
        }
      ]
    },
    "keybindings": [
      {
        "command": "opencode.button.open",
        "title": "Open opencode",
        "key": "ctrl+escape"
      },
      {
        "command": "opencode.button.new-session",
        "title": "New opencode session",
        "key": "ctrl+shift+escape"
      },
      {
        "command": "opencode.addFilepathToTerminal",
        "title": "opencode: Insert At-Mentioned",
        "key": "ctrl+alt+k",
        "mac": "cmd+alt+k",
        "win": "ctrl+alt+k",
        "linux": "ctrl+alt+k"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "bun run package",
    "sync-buttons": "node script/generate-manifest.mjs",
    "compile": "bun run sync-buttons && bun run check-types && bun run lint && node esbuild.js",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "bun run sync-buttons && bun run check-types && bun run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "bun run compile-tests && bun run compile && bun run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.94.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "eslint": "^9.25.1",
    "esbuild": "^0.25.3",
    "typescript": "^5.8.3",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2"
  }
}
